# Reference folder

Use this folder for notes about local references that may exist outside Git.

Suggested local-only material:
- downloaded Live API docs
- LOM snapshots
- maxdevtools clone
- SDKs or binaries with license restrictions

Recommended local subfolders (gitignored):
- `reference/local/`
- `reference/downloads/`
